#include <stdio.h>
#include <ctype.h>
#include <limits.h>
#include "getnum.h"
#include "bits.h"

int main() {
  /* int size = 6, arr[CHAR_BIT] = {'1','1','0','0','1','0','0','0'};
  rearrange(&arr, &size);
  for (int i = 0; i < CHAR_BIT; i ++) printf("%c", arr[i]);
  printf("\n"); */
  
  //printf("Number conversion: %ld\n", getnum());
}
